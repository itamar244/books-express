import { makeStyles } from "@material-ui/styles";

const bookItemStyles = makeStyles((theme) => ({
  root: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default bookItemStyles;
