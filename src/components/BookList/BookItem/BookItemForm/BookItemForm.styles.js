import { makeStyles } from "@material-ui/styles";

const bookItemFormStyles = makeStyles((theme) => ({
  input: {
    width: "7rem",
  },
  submitBtn: {
    backgroundColor: "#8a2b06",
    marginTop: "0.5rem",
  },
}));

export default bookItemFormStyles;
