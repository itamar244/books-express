import { makeStyles } from "@material-ui/styles";

const totalAmountStyles = makeStyles((theme) => ({
  root: {
    display: "flex",
    justifyContent: "space-between",
  },
}));

export default totalAmountStyles;
